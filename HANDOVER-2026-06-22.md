# 【最初に読む】SAMPLER STUDIO 引き継ぎ — 2026-06-22（MIDI/LED 大改修 → 次の担当者へ）

このファイルが最新の入口。**実装担当（別の人）向け**に、今日やったこと・現状・残作業・手順をまとめる。
- 一緒に作るユーザー＝**のむさん（コンサート演出家・コード未経験・日本語）**。結論から・舞台用語で・コマンドはコピペ完成形で渡すこと。
- 旧入口 `HANDOVER-MASTER-2026-06-21.md` と確定バグ全文 `BUGLIST-2026-06-21.md`（67件）も併読。本書は 2026-06-22 の差分と残作業の上書き。

本番URL: https://nrs2013.github.io/sampler-studio/ ／ リポ: `nrs2013/sampler-studio`（**単一HTML `index.html` を GitHub Pages・main直push配信**）／ ローカル `~/Documents/sampler-studio/`。現行 **APP_VERSION = v12.9.38**。

---

## 0. 一行サマリ
2026-06-22 に **MIDI/LED 系統を v12.9.31→v12.9.38 で全面整備**（本番事故防止・二度鳴り・マスター・**パッド色↔Grid LEDの色リンク根治**・他機材の誤爆防止）。全て本番反映済み・多エージェントで検証GO。**残るは ②DECK B消失（最重要）＋ BUGLIST #25〜67 の中小**。

---

## 1. 実機・ハードの確定事実（コードに無い／ライブ実測。これを前提に直す）
- コントローラ＝**Intech Grid**（USB1ポート"Grid"。**BU16**=16ボタン ＋ **EF44**=4エンコーダ+4フェーダ が積層、1ポートで両方流れる）。他に **MPC Studio（3入力ポート）** と **MIDI Keys** も常時接続。
- **BU16ボタン送信**: チャンネル13（status下位ニブル=12）・**ノート32〜47**（ボタンi = 32+i）・押下vel127/離上vel0（Note-Off 0x80ではなくvel0）。
- **BU16のLED**: **ノート36〜51受信で点灯**（ボタンi ← note 36+i）。＝**入力32-47とLED36-51で+4ズレ**（実機仕様）。
- **EF44**: フェーダ/エンコーダは **CC・チャンネル1**。System の `midirx_cb`(Lua) が受信ノートで自分のLEDを光らせ、かつ **ch16でノートを送り返すエコー**を出していた（二度鳴りの一因）。→ **Grid Editor で `note<=35` に編集してStore済**（フェーダ漏れ停止）。アプリ側でも ch16 を入口で破棄。
- アプリは従来「全MIDI入力を聴く・受信chを見ない」設計だった（誤爆の温床）。今回フィルタを追加。

---

## 2. 今日の改修（v12.9.31→v12.9.38・全て本番反映済み）
| ver | commit | 内容 |
|---|---|---|
| v12.9.31 | f333fcf | 本番事故防止：handleMIDI学習中は全入力飲込／パッド・バンク切替を `appMode==='sampler'` 限定（STOP/PAUSE/FADE OUTは全モード維持）／MIDIパッド常時フル／SCHEDULE自動発火を door 限定 |
| v12.9.32/33 | 09c77fb/6876913 | 二度鳴り抑制：同一ノートの90ms再発火を無視（`MIDI_RETRIG_MS`/`_lastMidiTrig`、キー＝`deckIdx:bank:note`） |
| v12.9.34 | 776b1fb | 診断用MIDIモニタ `?midimon=1`（右下に生MIDI表示・本番URLでは非動作） |
| v12.9.35 | 412779c | マスターフェーダーを全モードで本物の全体マスターに（`masterGain=state.masterVol`）。master CC→`setMasterVol`+`return`（二重発火停止 #25/#34） |
| v12.9.36 | 4c76ef7 | （※経緯）MIDI既定を一旦「空」にしたら色リンクが切れた → v12.9.37で是正 |
| v12.9.37 | 7fe7d8f | **色リンク根治**：`ledSend` を**位置ベース化**（pad index i → note 36+i を必ず送信＝入力割当に非依存で色が必ずリンク）。入力既定を **DEFAULT_NOTES=32-47**（BU16一致）に。`loadStateLocal` に冪等移行（旧36+i/空null→32+i）。**GRID IN**（Gridポートのみ聴く・学習中は全許可）／**MIDI⌫**（全MIDI割当一掃）。ch16エコー入口破棄。 |
| v12.9.38 | 4395389 | モード切替で Grid LED を能動再リンク/消灯（残光防止） |

**結果**：①Gridを叩くと正しいパッドが1回だけ鳴る ②パッドの色がGridボタンに必ずリンク ③DOOR/MULTI中やMPC/鍵盤で誤爆しない ④マスターが全体音量として効く。

### のむさんが決めた3点（反映済み）
1. EF44のLED＝「選ばれてる時だけ光る」→ パッド色の漏れを止め、EF44は基本消灯（Grid側 `note<=35`）。
2. パッドのノート＝**BU16と一致(32-47)**に。3. MIDIでpadを鳴らす音量＝**常にフル**。

### 検証
多エージェント監査（確定バグ32件/本番ブロッカー18件）＋色リンク総検証14項目＝**GO・回帰ゼロ**。ワークフロー雛形はセッションの `workflows/scripts/` に保存。

---

## 3. 現在のMIDI/LED設計（直す時の前提）
- `handleMIDI`：入口で `_midiMonLog` → **ch16なら return**（エコー破棄）→ 学習中は全入力飲込 → Note-On(vel>0)：`appMode==='sampler'` 時のみ **pad照合(`pad.midiNote===data1`)＋`deckIdx:bank:note`の90msデバウンス**→`playPad`（常にフル）。**パニック(FADE OUT/STOP/PAUSE)は全モード**。バンク切替はsampler。CC：**master.midiCC→`setMasterVol`+`return`** → 続いて deckVol ループ。
- `ledSend`：**位置ベース**。`bank.pads.forEach((p,i)=> send [ch13, 36+i, _ledVel(p)])`。空パッドは`_ledVel=0`で消灯。`appMode!=='sampler'`は送らない。`setActiveDeck/switchDeckBank/setAppMode/render` から再送（残光防止）。
- `updateMidiInputs`：**GRID INフィルタ**（`MIDI_IN_GRID_ONLY` 既定ON＝`/grid/i`のみ結線・学習中は全入力）。ヘッダーに `LED` / `MIDI⌫`(clearAllMidi) / `GRID IN`(toggleMidiInGrid) ボタン。
- 既定割当：`DEFAULT_NOTES=[32..47]`、`makePad` midiNote=32+i。controlAssign の master/fadeout/stop/pause の **MIDIは null（学習で入る）**・keyは保持。

---

## 4. 残作業（優先順）

### ★ ②DECK B消失（HIGH・最優先・本番データ事故）
**症状**：プリセット保存/読込・プロジェクト書き出し/読込が **DECK B（`state.samplerDecks[1]`）を捨てる**。`state.banks` は「今表示中デッキのエイリアス」なので、それだけ直列化/復元すると DECK B が消える。BUGLIST **#15〜#19** 該当。

**正解の形は既にある**：`saveStateLocal()`（現在 7718行）と `loadStateLocal()`（7767行）は **`samplerDecks` 全体を正しく直列化/復元**している。これと同じ形に他も揃える。

**直す箇所（関数名で grep。行は今後ズレるので関数で探す）**：
1. `savePreset()`（~7558）：presetData が `banks`(アクティブのみ)だけ。→ `samplerDecks` 全体（＋`hold`）を saveStateLocal と同形で保存。
2. `loadPresetByName()`（~7587）：`state.banks` だけに戻している。→ `preset.samplerDecks` があれば loadStateLocal のデッキ復元ブロックと同様に `state.samplerDecks` を再構築→`_applyDeckToState`→全デッキの音源を decode。`preset.banks` しか無い旧プリセットは deck0 へ入れる**レガシーfallback**を残す。
3. `exportProject()` の refId 収集（~6780台、`refIds` で grep）：`.ssproj` のメタ(samplerStudioState)には samplerDecks が入っているが、**音源バイトを詰める refIds が `st.banks` しか歩いていない**ため DECK B 専用音源が zip に入らない＝別Macで空。→ `st.samplerDecks` も歩いて `p.sampleId` を refIds に追加。
4. `saveProjectJSONToFolder()`（~6529、`state.sampler` ブロック）：`banks/currentBank/masterVol/fadeTime/fadeCurve` だけ。→ `samplerDecks`(各デッキbanks+deckVol+fade)・`hold`・`controlAssign`・`playMode`・`fadeBezier`・`currentDeckIdx` を saveStateLocal と同形で。
5. **読込(import)が2箇所**：`data.state.sampler` から `samplerStudioState` を復元する処理が **2つ**ある（フォルダ開き ~5058 と PWA起動 ~19560、`data.state.sampler` で grep）。両方に `samplerDecks` と `currentDeckIdx` をコピーして loadStateLocal のデッキ復元(`s.samplerDecks`)が走るように。
- **テスト手順**：DECK A/B 両方に音を入れる → プリセット保存→別名読込 / プロジェクト書き出し→別の場所で読込 → **DECK B の音が残る**こと。

**関連の同系バグ（できれば同時に）**：#13 loadSampleToPadでtrim持ち越し（新音源で `trimStart/trimEnd` をリセット）、#14 プリセット読込でghost audioBuffer（空プリセット枠で `audioBuffer=null` にしない）、#20 PAUSEデシンク（playPadでctx.resume時に`isPaused=false`）、#21 `_isAnyPlaying()` が `window.voices`(存在しない)を見て常にfalse（`_forEachPad`で`p.isPlaying`走査に）。

### その他（BUGLIST #25〜#67・MED/LOW）
`BUGLIST-2026-06-21.md` を重要度順に。例：#25 マスター二重減衰の残り、#34/#35 既存重複割当のサニタイズ（読込時に重複CC/noteを先勝ちでnull化）、#26 入力の二重バインド、#36 pad note と control note の名前空間衝突 等。今回の改修で消えた/緩和したものもあるので、各項目は現コードで再確認してから着手。

---

## 5. デプロイ手順（director-workflow・main直push）
```bash
cd ~/Documents/sampler-studio
# index.html を編集後、最大の<script>を抜いて構文チェック
python3 -c "import re;s=open('index.html').read();b=re.findall(r'<script(?![^>]*src=)[^>]*>([\s\S]*?)</script>',s);open('/tmp/s.js','w').write(max(b,key=len))"
node --check /tmp/s.js && echo OK
# APP_VERSION を上げる（本番反映の目印）→ commit → push
git add index.html && git commit -m "v12.9.xx: ..." && git push origin HEAD
# 反映確認: curl -s "https://nrs2013.github.io/sampler-studio/index.html?cb=$(date +%s)" | grep -c "v12.9.xx"  → 1以上=live
```
- 反映は1〜2分。**のむさんは ⌘+Shift+R で強制リロード**（PWAだが SW は network-first なので強制リロードで最新が来る）。
- 認証情報（PAT/SSH鍵）はのむさんのローカルに保管（director-workflow：PAT/SSHどちらでもmain直push可）。※具体的な保管場所はここには書かない（公開リポのため）。

## 6. 検証ツール
- アプリ内 **`?midimon=1`**：右下に生MIDI（ON/OFF/CC・ch・note・vel・入力ポート名）。原因切り分けに最強。本番URL(?なし)では動かない。
- Grid実測：`swift /tmp/midiwatch.swift` 等（CoreMIDI監視。/tmp に無ければ作る）。Grid Editorは遠隔GUIが不安定なので**のむさん操作を1ステップずつ**＋Discard Allで戻せる。

## 7. 重要メモ
- のむさんは**コード未経験・日本語・音声入力（文字化け混じる）**。**データ消失を極度に嫌う**ので、保存形式を変える②は特に慎重に（旧ファイルが読めるレガシーfallback必須・テストしてから本番）。
- Grid のハード設定（BU16/EF44のノート・Lua）は **Grid Editor 1.6.8** 側。詳細は `SAMPLER-STUDIO-GridLED-引き継ぎ-2026-06-21.md`。
- 「動かない」と言われたら自分で3段階（①コンソール/構文 ②ハンドラのログ ③DOM/実機）潰してから報告。

---
— 2026-06-22 / 前任（のむさん×Claude）。MIDI/LED は決着、次は ②DECK B消失 から。
